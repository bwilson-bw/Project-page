import React from 'react';
import Typography from '@material-ui/core/Typography';

const Rxjs = () => (
  <div className="home-body">
    <Typography variant="h1">Pixi JS</Typography>
  </div>
);

export default Rxjs;
