import React from 'react';
import Typography from '@material-ui/core/Typography';

const PageNotFound = () => (
    <div className="home-body">
        <Typography variant="h1">Page not found m8</Typography>
    </div>
);

export default PageNotFound;
